/*
function handleResize() {
          if (window.innerWidth <= 768) {
            // Code pour écran petit
            console.log('Écran est inférieur ou égal à 768px');
          } else {
            // Code pour écran grand
            console.log('Écran est supérieur à 768px');
          }
        }
        
        // Ajoute un écouteur d'événement pour le redimensionnement de la fenêtre
        window.addEventListener('resize', handleResize);
        
        // Appelle la fonction une fois au chargement pour gérer l'état initial
        handleResize();
*/    
$(document).ready(function() {
})  